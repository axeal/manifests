apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup-all
  namespace: longhorn-system
spec:
  task: "backup"
  cron: "0 2 * * *"  # Daily at 2AM
  groups:
  - default
  retain: 28  # Keep 28 backups
  concurrency: 1
  full-backup-interval: 7  # Full backup every 7 days
